# Local secrets and scratch. The API key lives outside this tree.
*.local.*
*api-key*
*apikey*
*token*
.env
.env.*
secrets/
.cache/
*.log

# Agent/BMAD workflow trees stay on disk for local development but
# must not ship in the published plugin checkout.
.agents/
.claude/
.cursor/
_bmad/
_bmad-output/
